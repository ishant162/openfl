settings:
  listen_host: localhost
  listen_port: 50050
  envoy_health_check_period: 5  # in seconds
  review_experiment: True

aggregator:
  callable_func:
    settings: {}
    template: private_attributes.aggregator_private_attrs.aggregator_private_attrs

